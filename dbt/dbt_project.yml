name: wikipedia_page_activity
version: 1.0.0
config-version: 2

profile: wikipedia_page_activity

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]

target-path: target
clean-targets:
  - target
  - dbt_packages

models:
  wikipedia_page_activity:
    +materialized: view

    staging:
      +schema: staging
      +materialized: view
