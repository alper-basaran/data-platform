# Wikipedia API Extract Job

## Local setup

Use Python 3.12 (or any Python >=3.10,<3.14) for this project.

```bash
poetry env use python3.12
poetry install
```

If Poetry already created a Python 3.14 environment, remove it first and recreate:

```bash
poetry env remove --all
poetry env use python3.12
poetry install
```